IMPORTANTE: Para arrancar esta aplicacion servidor:

1- Moverse al directorio "2da_pre-entrega"
2- Ejecutar npm install
3- Ejecutar npm start


Nota: En la ruta src\config\getSystemConfigurationParameters.js, puede verificar con cual funcionalidad de BD se ejecuta la aplicacion.

se añade archivo comprimido en "base.rar", donde se encuentra la coleccion en MONGODB

En mi pc, inicio mongo en consola, mediante el siguiente comando "mongod --dbpath "D:\germa\Downloads\desafio9\base"

Ingresar la ruta que corresponda una vez se almacene en equipo local y obtenga la nueva ruta"

